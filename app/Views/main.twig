{% extends "layout/layout.twig" %}

{% block content %}

<div class="row">
    {% for image in images %}
        <div class="col-md-4">
            <p>{{ image.id }}</p>
            <img src="{{ image.src }}" alt=""/>
        </div>
    {% endfor %}
</div>
{% endblock %}